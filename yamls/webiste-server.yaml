apiVersion: v1
kind: Serivce
metadate:
  name: website-service
  labels:
    app: nginx
spec:
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
